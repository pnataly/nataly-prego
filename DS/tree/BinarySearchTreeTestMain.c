#include <stdio.h>

int main()
{
	PrintResult(CreateTree1(), "CreateTree1");

	PrintResult(DestroyTree1(), "DestroyTree1");

	PrintResult(InsertTree1(), "InsertTree1");

	PrintResult(InsertTree2(), "InsertTree2");

	PrintResult(InsertTree3(), "InsertTree3");

	PrintResult(IsDataFound1(), "IsDataFound1");

	PrintResult(IsDataFound2(), "IsDataFound2");

	return 0;
}
